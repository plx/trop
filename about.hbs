# Third-Party Licenses

This file is auto-generated by `cargo-about` and lists the licenses of every
third-party crate that ships with the `trop` / `trop-cli` distribution
(including build and development dependencies).

Do not edit by hand. Regenerate with `just licenses`. The CI job
`licenses` enforces that this file matches what `cargo-about` would produce.

`trop` itself is dual-licensed under your choice of `Apache-2.0` or `MIT`.
See `LICENSE`, `LICENSE-APACHE`, and `LICENSE-MIT` for the full text.

## Overview

{{#each overview}}
- **{{{name}}}** — {{count}} crate(s)
{{/each}}

## Licenses

{{#each licenses}}
### {{{name}}}

Used by:

{{#each used_by}}
- [{{{this.crate.name}}} {{this.crate.version}}]({{{this.crate.repository}}})
{{/each}}

```
{{{text}}}
```

{{/each}}
