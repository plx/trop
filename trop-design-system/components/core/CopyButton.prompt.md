**CopyButton** — copies a command to the clipboard and flips to "Copied" for ~2s. Pair it with a `CommandCard`.

```jsx
<CopyButton text="cargo install trop-cli" />
```

Outlined (panel fill, hairline border); icon swaps clipboard → check on success.
